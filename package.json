{
  "name": "personal-knowledge-base",
  "version": "1.0.0",
  "description": "Personal Knowledge Base System for Cloudflare",
  "scripts": {
    "build": "npm run build:client",
    "build:client": "mkdir -p public && echo 'Building client files...'",
    "dev": "wrangler pages dev public --kv KNOWLEDGE_BASE"
  },
  "devDependencies": {
    "wrangler": "^3.18.0"
  }
}
